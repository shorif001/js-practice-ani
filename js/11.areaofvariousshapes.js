// Formula Sheet for area of 2d Shapes https://www.cazoommaths.com/

// square & Rectangle = Base * Height
// Triangle = Base * perpendicular height + 2
// Trapezium = (a+b) * height 2
// Parallelogram = Base * Perpendicular Height
// Rhombus = Length * Height / 2
// Kite = Length * Height / 2

//square & rectangle area

// var base = prompt("Enter Base = ");
// var height = prompt("Enter height = ");

// var area = base * height;

// document.write("Area = " + area);

// float a convert korar jonno

// var base = parseFloat(prompt("Enter Base = "));
// var height = parseFloat(prompt("Enter height = "));

// var area = base * height;

// document.write("Area = " + area);

//Triangle area

var base = parseFloat(prompt("Enter Base = "));
var height = parseFloat(prompt("Enter height = "));

var area = (base * height) / 2;

document.write("Area = " + area);
